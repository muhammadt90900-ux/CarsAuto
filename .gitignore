# ── Dependencies ──────────────────────────────
node_modules
.pnp
.pnp.js

# ── Build outputs ─────────────────────────────
dist
.next
.turbo
build
out

# ── Environment — NEVER commit secrets ────────
.env
.env.local
.env.*.local
.env.production
.env.staging
# Keep example files:
!.env.example
!.env.local.example

# ── Logs ──────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS ────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ───────────────────────────────────────
.vscode
.idea
*.swp
*.swo

# ── Test / Coverage ───────────────────────────
coverage
.nyc_output

# Production secrets — never commit
.env.production
.env.local
.env.*.local
apps/api/.env
apps/web/.env.local

# Backups
backups/
*.sql
*.sql.gz

# Dist / build artifacts
apps/api/dist/
apps/web/.next/

# Turbo cache
.turbo/cache/
